See the top-level [README.md](../README.md) for usage. This directory contains
the runner code itself (`config.py`, `prepare_inputs.py`, `intrinsics.py`,
`runner_singleshot.py`, `runner_streaming.py`, `cli.py`, `gui.py`) and example
configs under `configs/`.
